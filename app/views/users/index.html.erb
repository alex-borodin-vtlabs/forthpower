<div id="users" class="infinite" data-url="<%= users_path %>">
  <% if @users.present? %>
    <%= render @users %>
  <% else %>
    <p>There are no users containing the term(s) <%= params[:search] %>.</p>
  <% end %>
</div>
